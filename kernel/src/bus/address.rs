
#[derive(Debug, Clone, Eq, PartialEq, Hash)]
pub struct Address {
    // addr: String,
}

impl Address {
    pub fn new(name: &str) -> Self {
        Self {
            // addr: name.to_owned(),
        }
    }
}
